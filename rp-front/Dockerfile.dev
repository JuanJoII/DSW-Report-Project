FROM oven/bun:alpine
WORKDIR /app

# Borrar lock antiguo si existe para forzar resolución fresca
COPY package.json ./
RUN bun install

COPY . .

# Asegurar que las dependencias estén sincronizadas
RUN bun install

EXPOSE 5173
CMD ["bun", "run", "dev", "--", "--host", "0.0.0.0"]